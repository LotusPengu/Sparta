# Variables

"""
It is important that variables are organised in an efficient way
    * No variable name can start with a literal (core data types)
    * If more than one word then use _ - snakespacing
    * Code runs linearly, so function names can be changed if placed after the first instance

"""
a = 4
b = 4.5

print(type(a))
print(a + b)